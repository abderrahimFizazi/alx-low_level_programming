#include <stdio.h>
/**
 * puts_half - reset argument to 98
 * @s: pointer
 * Return: void
 */

void puts_half(char *s)
{
  int i;

for (i = 0; str[i] != '\0'; i++)
;
i++;
for (i /= 2; str[i] != '\0'; i++)
{
_putchar(str[i]);
}
_putchar('\n');
}
