#include "main.h"
#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * add - - prints _putchar, followed by a new line
 * @n : character to check the case
 * @y : character to check the case                                                               
 * Return:0 or 1 or -1
 */
int add(int n, int y)
{
return (n + y);
}
